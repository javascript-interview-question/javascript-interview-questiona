import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'JS Interview Prep - Master JavaScript Concepts',
  description: 'Comprehensive JavaScript interview preparation with interactive code examples and detailed explanations. Perfect for developers preparing for technical interviews.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}